# Credentials — never commit these
.env
config/secrets/*
!config/secrets/.gitkeep
config/certs/*
!config/certs/.gitkeep
config/2fa-tokens.json
config/audit.log
config/.new-password

# User config — copy items.yml.sample to items.yml to get started
config/items.yml

# Dependencies
node_modules/
backend/node_modules/
frontend/node_modules/

# Build output
frontend/dist/

# Misc
.DS_Store
